extends ../layout

block content
	.container
		.row
			each item in movies
				.col-md-2
					.thumbnail
						a(href='/movie/#{item._id}',role='button')
							img(src="#{item.poster}",title="#{item.title}")
							h3 #{item.title}
							p:a.btn.btn-primary(href="/movie/#{item._id}",role='button')观看预告片
							//- p:a-->To save space, jade provides an inline syntax for nested tags.